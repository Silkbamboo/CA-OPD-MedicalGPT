# Public result artifacts

These files are compact, de-identified summaries reconstructed from the frozen experiment
reports. They contain aggregate counts, paired statistics, protocol identities, and final
access counters only.

They intentionally do **not** contain prompts, labels, model outputs, rollouts, weights,
checkpoints, local paths, or private platform metadata. `SHA256SUMS` is generated from the
reviewed files during the release check.
