[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    gpu: requires a CUDA device (skipped on CPU-only boxes)
    needs_model: requires local model weights via CA_OPD_MODEL_PATH
